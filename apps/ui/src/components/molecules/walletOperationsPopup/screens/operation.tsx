import React from 'react';
import OperationsStake from './operations/stake';

const WalletOperationsPopupOperation = (props) => {
    return <OperationsStake {...props} />;
};

export default WalletOperationsPopupOperation;
